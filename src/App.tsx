import * as React from 'react';
import { TodoContainer, MessageContainer } from './containers';

export class App extends React.Component {
  render() {
    return (
        <TodoContainer/>
    );
  }
}

